---
name: Bug report
about: Something isn't working correctly
labels: bug
---

## Environment

- **OS:** (e.g. macOS 14.4, Ubuntu 22.04, Windows 11)
- **Docker version:** (`docker --version`)
- **SpeciesNet version:** (`python -c "import speciesnet; print(speciesnet.__version__)"`, or "not installed / using subprocess adapter")
- **Browser:** (e.g. Chrome 124, Firefox 125, Safari 17)
- **How are you running Studio?** `docker compose up` / without Docker / other

## Steps to reproduce

1.
2.
3.

## What you expected

## What actually happened

(Include any error messages from the browser console or Docker logs. `docker compose logs backend` often shows the root cause.)

## Minimal predictions JSON snippet

If the bug is related to parsing or displaying inference results, paste a small excerpt (2–5 items) from your SpeciesNet predictions JSON. Redact any file paths or personal information.

```json

```

## Screenshots

If applicable, add a screenshot showing the problem.
